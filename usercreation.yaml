---
 - hosts: all
   gather_facts: False
   vars_prompt: 
              - name: username
                prompt: Enter username
                private: No
   tasks:
              - name: Create User
                user:
                    name: "{{ username }}"
                    state: present
